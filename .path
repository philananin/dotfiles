PATH=/usr/bin:/bin:/usr/sbin:/sbin
export PATH

# local installations
PATH=/usr/local/bin:/usr/local/sbin:"$PATH"

# home
PATH="$PATH":~/bin

case "$OSTYPE" in
    cygwin*)
        source ~/.path-cygwin
        ;;
    darwin*)
        source ~/.path-osx
        ;;
esac
